import random 
def main():
    printChars(ch1, ch2, numberPerLine)
def printChars(ch1, ch2, numberPerLine):
    #Initalize count
    count = 0
#Check line range
        for i in range(10):
            count += 1
main()
