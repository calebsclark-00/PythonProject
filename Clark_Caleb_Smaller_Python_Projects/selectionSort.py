def selectionSort(1st):

    #in the list which controls varaible
    for i in range(len(1st)):

        #Find the minimum in the list
        currentMin = 1st[i]
        currentMinIdex = 1

        #Select the smallest element in the list
        for j in range(i + 1, len(1st)):
            if currentMin
